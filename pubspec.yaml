# TODO: Update Name
name: project_base
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0

  animations: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  effective_dart: ^1.2.4
  import_sorter: ^4.4.1

flutter:
  uses-material-design: true
